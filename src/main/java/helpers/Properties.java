package helpers;

import org.aeonbits.owner.ConfigFactory;

public class Properties {
    /**
     * Создание конфигураций проперти
     */
    public static ConfProperties confProperties = ConfigFactory.create(ConfProperties.class);
}
